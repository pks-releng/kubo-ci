platform: linux

image_resource:
  type: docker-image
  source:
    repository: pcfkubo/kubo-ci
    tag: stable

run:
  path: git-kubo-ci/scripts/update-kubernetes-port.sh

inputs:
- name: kubo-lock-pre

outputs:
- name: kubo-lock
